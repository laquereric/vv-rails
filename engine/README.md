# vv-rails / engine

Aggregate layer — Rails Engine with ActionCable integration.

## Contents

- `Engine` — Rails::Engine, isolate_namespace
- `VvChannel` — ActionCable channel for plugin communication
- `InstallGenerator` — `rails g vv:install` generator
- Routes, gemspec
